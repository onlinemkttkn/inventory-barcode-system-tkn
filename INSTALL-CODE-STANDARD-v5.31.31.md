# มาตรฐาน QR / Barcode v5.31.31

สถานะ: แก้ไฟล์ใน workspace และทดสอบ local แล้ว ยังไม่ได้ deploy เว็บหรือ Supabase

## กติกา

- Barcode สินค้าใหม่บนฉลากใช้ `product_code` ที่บันทึกจริง
- QR สินค้าใช้ `TKN-P-` ต่อด้วยรหัสเดียวกัน
- ไม่คำนวณรหัสฉลากใหม่ตามต้นทุนเมื่อพิมพ์; การสร้างล็อตยังเป็นขั้นตอนแยก
- QR กล่องใช้รหัสกล่องเต็ม `TKN-B-...` ตามระบบเดิม
- คงเลขศูนย์นำหน้า ตัวพิมพ์เล็ก/ใหญ่ และเครื่องหมายในรหัส
- กล้องและ USB แยกค่าจาก URL ด้วยลำดับเดียวกัน:
  `scan`, `code`, `barcode`, `sku`, `tracking`, `tracking_number`, `id`
- URL ที่ไม่มีพารามิเตอร์เหล่านี้คงไว้ทั้งค่า ไม่เดาว่าส่วนท้าย URL คือ SKU
- พบสินค้ามากกว่าหนึ่งรายการจากรหัสเดียวกันให้แจ้งข้อผิดพลาด
- ไม่ตัดรหัสล็อตไปใช้ SKU หลักอัตโนมัติ

## ติดตั้ง

1. ใน Supabase โปรเจกต์ `wkozeuxyhqcmiatssviq` รัน
   `sql/2026-09-16-code-data-preflight.sql` แบบอ่านอย่างเดียว ตรวจสิทธิ์อ่าน
   dependencies และรหัสกำกวมตามคู่มือ `docs/2026-09-16-code-lookup-install.md`
2. รัน `sql/INSTALL-CODE-STANDARD-v5.31.31.sql` ให้สำเร็จก่อนอัปโหลดเว็บ
   ไฟล์เดียวติดตั้งตัวค้นรหัสและ trigger ตรวจสินค้าก่อนลงกล่องใน transaction เดียว
   หากขึ้น LOOKUP_DEFINITION_CHANGED ให้ ROLLBACK แล้วตรวจนิยามใหม่
   ห้ามลบ guard เพื่อบังคับรัน ไม่มีการแก้รหัสสินค้า/ยอดสต็อกใน migration นี้
   จากนั้นรัน `sql/2026-09-16-code-standard-verify.sql` ต้องได้ passed=true ทั้ง 3 แถว
3. อัปโหลดไฟล์ตาม `UPLOAD-CODE-STANDARD-v5.31.31.txt` โดยรักษาโครงสร้างโฟลเดอร์
4. เปิดหน้าเว็บใหม่และทดสอบด้วยบัญชีที่ใช้จริงตามรายการด้านล่าง

อย่าอัปโหลด `.tools` หรือ fixture/tests ไปเป็นไฟล์เว็บไซต์
หากย้อนกลับ ต้องย้อนทั้ง frontend และ SQL ให้เป็นรุ่นก่อนหน้า
SQL rollback ทั้งตัวตรวจกล่องและตัวค้นอยู่ที่ `sql/ROLLBACK-CODE-STANDARD-v5.31.31.sql`

## ตรวจรับ

เลือกสินค้าที่มีรหัสไม่ซ้ำหนึ่งรายการ แล้วสแกน Barcode กับ QR ของสินค้านั้น
ในหน้า scanner, mobile stock, POS, รับ/จ่าย, โอน, ตรวจนับ, กล่อง, คัดแยก และใบสั่งซื้อ
ต้องได้ product ID เดียวกัน โดยยังจำกัดสต็อกตามสาขาและสิทธิ์เดิม
ทดสอบผ่านกล้องและ USB ทั้งขณะ focus ช่องกรอกและไม่ได้ focus
ทดสอบฉลากใหม่ด้วยเครื่องอ่านจริงและตรวจขนาดการพิมพ์จริง

Barcode โรงงานและ barcode master ถูกอ่านตาม RLS เดิม; ผู้ไม่มีสิทธิ์อ่าน mapping
จะไม่เห็น mapping นั้น ไม่มีการขยายสิทธิ์อัตโนมัติ
ฉลากเก่าที่ตรงกับรหัสบันทึกจริงยังอ่านได้ ส่วนฉลากที่รุ่นเดิมสร้างรหัสต้นทุน
ขึ้นบนหน้าเว็บโดยไม่ได้บันทึกเป็นรหัส/alias ต้องตรวจและพิมพ์ใหม่ ไม่เดาจับคู่ข้ามล็อต

## ขอบเขต

แก้ shared product pattern, unified scanner, การค้นสินค้าในหน้าหลัก และ version
ของ asset/cache; JsBarcode หน้าใช้งานหลักใช้ 3.12.1 เหมือนกัน
ขนาดฉลากยังเลือกตามเครื่องพิมพ์และรูปแบบงานได้ ใบเสร็จและ tracking ยังคงรหัสของงานนั้น
ไม่ได้แก้ RLS หรือย้ายประวัติกล่อง และไม่ได้แทนที่ชุด archive CLEAN-PACK เก่า

## ตรวจสินค้าก่อนลงกล่อง

- หน้า Box QR ตรวจพบสินค้าเพียงรายการเดียว เปิดใช้งาน และมีรหัสจริงก่อนบันทึก
- หน้าแยกสินค้า ตรวจ/บันทึกสินค้าให้มี product ID ก่อนเพิ่มเข้าคิวกล่องและฉลาก
- ถ้าแก้ SKU หรือ product ID ของรายการในคิว จะถอดออกจากคิวจนกว่าจะตรวจใหม่
- ฐานข้อมูลตรวจ INSERT/UPDATE ใน stock_box_items ซ้ำ ไม่รับ QR กล่องเป็นสินค้า
  ไม่รับรหัสที่ชี้คนละ product ID หรือสินค้าปิดใช้งาน
- บันทึกลงฐานข้อมูลไม่ผ่าน ไม่เพิ่มจำนวนกล่องในเครื่อง; ไม่รับรายการซ้อนระหว่างบันทึก
- ลดจำนวน/เอาสินค้าเดิมออกได้แม้ภายหลังสินค้าถูกปิดใช้งาน

ผล local: 89 กรณีผ่าน (browser 17, client lookup 17, lookup SQL 24,
packing SQL 16, scanner 4, box flow 6, sorting flow 5)
พร้อม syntax checks; ยังไม่ใช่ผล UAT บน hosted Supabase หรือกล้อง/เครื่องพิมพ์จริง

# Install Hotfix

Upload to the repository only if you keep the bridge there for distribution.
For the cashier Windows PC, replace the local `printer-bridge` folder with the
folder in this package.

No SQL is required.

After replacement, run `START-RONGTA-BRIDGE.bat` as Administrator.
